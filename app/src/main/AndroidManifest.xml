<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_sec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_SEC">
        <activity android:name=".DesignSuggestion2"></activity>
        <activity android:name=".DesignSuggestion1" />
        <activity android:name=".AutoLogo3Activity" />
        <activity android:name=".AutoLogo2Activity" />
        <activity android:name=".AutoLogo1Activity" />
        <activity android:name=".AutoLogoActivity" />
        <activity android:name=".Activity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>